// @flow 
import * as React from 'react';
import { Page } from '../../components/Page';
import Form from './Form';

const PageForm = () => {
    return (
        <Page title="Cadastro de gêneros">
            <Form />
        </Page>
    );
};

export default PageForm;